# This will add the @lombok.Generated annotation to all the code generated by Lombok
# This may exclude it from coverage by jacoco
lombok.addLombokGeneratedAnnotation = true

# Set the exceptionType to JDK, so lombok will not generate an if-statement and check the null-ness
# via "Objects.requireNonNull()", instead of a separate if instruction.
# This is mainly done for test coverage reports by jacoco.
lombok.nonNull.exceptionType = JDK